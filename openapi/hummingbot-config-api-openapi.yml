openapi: 3.2.0
info:
  title: Hummingbot Gateway /config API
  description: API endpoints for interacting with DEXs and blockchains
  version: dev-2.11.0
servers:
- url: http://localhost:15888
tags:
- name: Config
  description: System configuration endpoints
paths:
  /config/:
    get:
      tags:
      - Config
      description: Get configuration settings. Returns all configurations if no parameters are specified. Use namespace to get a specific config (e.g., server, ethereum-mainnet, solana-mainnet-beta, uniswap).
      parameters:
      - schema:
          type: string
        examples:
          server:
            value: server
          ethereum-mainnet:
            value: ethereum-mainnet
          solana-mainnet-beta:
            value: solana-mainnet-beta
          uniswap:
            value: uniswap
        in: query
        name: namespace
        required: false
        description: Optional configuration namespace (e.g., "server", "ethereum-mainnet", "solana-mainnet-beta", "uniswap")
      responses:
        '200':
          description: Default Response
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
      summary: Get config
      x-summary-source: derived
      operationId: getConfig
      x-operation-id-source: derived
  /config/update:
    post:
      tags:
      - Config
      description: Update a specific configuration value
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                namespace:
                  description: Configuration namespace (e.g., "server", "ethereum-mainnet", "solana-mainnet-beta", "uniswap")
                  type: string
                  example: server
                path:
                  description: Configuration path within the namespace (e.g., "nodeURL", "manualGasPrice")
                  type: string
                  example: nodeURL
                value:
                  description: Configuration value
                  anyOf:
                  - type: string
                  - type: number
                  - type: boolean
                  - type: object
                    properties: {}
                  - type: array
                    items: {}
              required:
              - namespace
              - path
              - value
            examples:
              example1:
                value:
                  namespace: solana-mainnet-beta
                  path: maxFee
                  value: 0.01
              example2:
                value:
                  namespace: ethereum-mainnet
                  path: nodeURL
                  value: https://eth-mainnet.g.alchemy.com/v2/your-api-key
              example3:
                value:
                  namespace: ethereum-mainnet
                  path: gasLimitTransaction
                  value: 3000000
              example4:
                value:
                  namespace: solana-devnet
                  path: retryCount
                  value: 5
              example5:
                value:
                  namespace: server
                  path: port
                  value: 15888
        required: true
      responses:
        '200':
          description: Default Response
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    description: Status message
                    type: string
                required:
                - message
      summary: Create config update
      x-summary-source: derived
      operationId: postConfigUpdate
      x-operation-id-source: derived
  /config/chains:
    get:
      tags:
      - Config
      description: Returns a list of available blockchain networks supported by Gateway.
      responses:
        '200':
          description: Default Response
          content:
            application/json:
              schema:
                type: object
                properties:
                  chains:
                    type: array
                    items:
                      type: object
                      properties:
                        chain:
                          type: string
                        networks:
                          type: array
                          items:
                            type: string
                      required:
                      - chain
                      - networks
                required:
                - chains
      summary: Get config chains
      x-summary-source: derived
      operationId: getConfigChains
      x-operation-id-source: derived
  /config/connectors:
    get:
      tags:
      - Config
      description: Returns a list of available DEX connectors and their supported blockchain networks.
      responses:
        '200':
          description: Default Response
          content:
            application/json:
              schema:
                type: object
                properties:
                  connectors:
                    type: array
                    items:
                      type: object
                      properties:
                        name:
                          type: string
                        trading_types:
                          type: array
                          items:
                            type: string
                        chain:
                          type: string
                        networks:
                          type: array
                          items:
                            type: string
                      required:
                      - name
                      - trading_types
                      - chain
                      - networks
                required:
                - connectors
      summary: Get config connectors
      x-summary-source: derived
      operationId: getConfigConnectors
      x-operation-id-source: derived
  /config/namespaces:
    get:
      tags:
      - Config
      description: Returns a list of all configuration namespaces available in Gateway.
      responses:
        '200':
          description: Default Response
          content:
            application/json:
              schema:
                type: object
                properties:
                  namespaces:
                    type: array
                    items:
                      type: string
                required:
                - namespaces
      summary: Get config namespaces
      x-summary-source: derived
      operationId: getConfigNamespaces
      x-operation-id-source: derived
